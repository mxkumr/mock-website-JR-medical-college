/** Placeholder route for pages that are not built yet. */
export const UNDER_CONSTRUCTION = "/under-construction";

/** Homepage section anchors — prefixed with / so they work from any route. */
export const HOME = {
  about: "/#about",
  contact: "/#contact",
  departments: "/#departments",
  services: "/#services",
} as const;
